# Local Development - No Kafka, No Database (will use in-memory for testing)
PORT=8080
KAFKA_ENABLED=false

# Optional: Add PostgreSQL if you have it installed locally
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/connectfour?sslmode=disable
